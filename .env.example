# Discogs personal access token (discogs.com/settings/developers)
DISCOGS_TOKEN=

# eBay app keys for the Browse API (developer.ebay.com — create an app, use the
# production Client ID/Secret). Optional: estimate-price falls back to
# Discogs-only if these are unset.
EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=
